<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><strong><%= link_to task.title, task_path(task) %></strong></li>
    <li><%= task.details %></li>
    <li><%= task.completed %></li>
    <br>
  <% end %>
</ul>
